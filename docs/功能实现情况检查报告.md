# 《旅馆·侵蚀日》功能实现情况检查报告

> 检查基准：`《旅馆·侵蚀日》（暂定）策划案——问剑gj.docx`（v1.4）
> 检查方式：10 个 reviewer 并行对代码库进行实现状态核对（仅区分「已实现/部分实现/未实现」，不检查 bug 细节）
> 项目根目录：`E:\UnityProjects\260725GJ`

## 总览

| 系统 | 状态 | 一句话结论 |
|------|------|-----------|
| 存档系统 | ✅ 全部实现 | 3 槽位 + 黎明自动存档 + 覆盖写入防 SL + 手动选择载入/删除，闭环完整；事件历史/Buff 深拷贝与还原校验已实现 |
| 审查招募 | 🟡 核心可用 | 候选数据/弹窗/招募拒绝/1-3 天来访均到位；缺白天侵蚀衰减、侵蚀渐进暴露、问答交互 |
| 侵蚀度与阵营 | 🟡 基础就绪 | 0-100 数值、三色判定、手动插旗已实现；缺真实颜色揭示、被动侵蚀结算 |
| 时间系统 | 🟡 框架就绪 | 四阶段循环 + 推进按钮已实现；缺 30 天强制结束、自动跳过日志、活跃类型效果 |
| 事件系统 | 🟡 框架就绪 | 触发引擎/条件评估/效果结算/UI 完整，27 夜间 + 18 日间资产到位，队列/限次重试/重入保护/Buff 生命周期/结算守卫已闭环；缺特殊 NPC/个人事件/叙事链 |
| 房间与经营 | 🟡 部分 | 拖拽分配/换房 + 食物每日消耗 + 双人房容量（1/2）与同住头像布局已实现；缺渐进解锁、职业产出、隔离策略、设施耐久度 |
| 物资/资源 | 🟡 部分 | 食物 + 货币已实现（货币含资源初始化与结算负担校验，药品已按策划移除）；缺道具系统 |
| 能力标签与工作 | 🔴 基本未实现 | 仅 10 种标签枚举 + JobId 空壳；岗位/产出/分配/奖惩全缺（标签仅用于事件条件与选项守卫） |
| 驱逐系统 | 🔴 完全未实现 | 无驱逐入口/Change/Reducer/反应逻辑 |
| 结算与结局 | 🔴 逻辑全空 | 仅 RunSummaryState 数据结构；结算/结局/奖章/多周目全缺 |

## 各系统详细情况

### 1. 时间系统（部分实现）

| 功能 | 状态 | 说明 |
|------|------|------|
| 四阶段划分（黎明→白昼→黄昏→黑夜，循环 30 天） | 部分实现 | 枚举与循环推进已实现（`GamePhaseManager.cs:97-107`）；**无 30 天强制结束机制** |
| 推进规则（有决策强制停留，无事件自动跳过） | 部分实现 | `CanAdvancePhase()` 强制停留已实现；自动跳过仅对黎明/黄昏隐藏阶段生效，白昼/黑夜仍需手动推进；**无「一切平静」日志记录** |
| 推进按钮 | 已实现 | `NextPhaseButton.cs` + `NextPhasePanel.cs` 完整 |
| 日夜活跃类型（日行/夜行/全天） | 部分实现 | 枚举与 UI 展示已实现；**无任何效率/惩罚游戏效果逻辑** |

### 2. 审查招募（核心可用）

| 功能 | 状态 | 说明 |
|------|------|------|
| 访客到来（每 1-3 天 1-3 名，任意时段） | 部分实现 | 间隔与人数已实现（`VisitorArrivalScheduler.cs`）；**仅随机 Dawn/Dusk，不含 Day/Night**；**无白天侵蚀衰减** |
| 申请者信息展示 | 已实现 | 姓名/肖像/描述/能力标签/活跃类型完整，侵蚀度正确隐藏 |
| 招募/拒绝交互 | 已实现 | 弹窗 + 确认/拒绝 + 容量检查完整 |
| 招募后侵蚀度逐渐暴露 | 部分实现 | 初始 0-40 随机已实现；**无渐进暴露机制**（当前直接写入真实值） |
| 问答交互（可选） | 未实现 | 无任何问答/对话流程 |

### 3. 能力标签与工作（基本未实现）

| 功能 | 状态 | 说明 |
|------|------|------|
| 10 种能力标签 | 部分实现 | 枚举完整（含司机/教师，超出策划 8 种）；中文显示名已配 |
| 工作岗类型与产出 | 未实现 | 仅 `JobId` 字段 + `AssignJobChange` 空壳，无岗位定义/产出逻辑 |
| 工作分配交互 | 未实现 | 无「今日安排」/工作列表 UI，现有面板仅支持房间分配 |
| 产出规则（匹配 +20%/-20%） | 未实现 | 无匹配奖惩计算逻辑 |
| 侵蚀度影响效率（绿 100%/黄 -10%/红 +20%） | 部分实现 | 三色分档已实现，但仅用于事件条件判定，**未挂钩工作效率** |

### 4. 侵蚀度与阵营（基础就绪）

| 功能 | 状态 | 说明 |
|------|------|------|
| 侵蚀度数值（0-100 隐藏，初始 0-40） | 已实现 | 完整 |
| 颜色标签自动判定（绿/黄/红） | 已实现 | 阈值与策划一致（`EventConditionEvaluator.cs:25-30`） |
| 玩家手动插旗 | 已实现 | flag 下拉 + 头像彩色环，独立于系统判定 |
| 真实颜色揭示 | 未实现 | 无夜间事件揭示/精神分析仪/侵蚀≥80 自动揭示 |
| 侵蚀度变化因素 | 部分实现 | 事件驱动通道已打通；**同住（SameRoomOtherTenants）效果目标已实现**（事件/Buff 可对同住者生效）；同层/隔离的被动侵蚀结算仍缺失 |

### 5. 房间与经营（部分）

| 功能 | 状态 | 说明 |
|------|------|------|
| 侧面视角布局 | 部分实现 | 多层映射 + 头像 + 颜色环已实现；**双人房双头像水平布局已实现**（`RoomAvatarSlotLayoutController` 按入住数在单一世界锚点两侧水平排列两个 `RoomTenantAvatarSlot`）；不显示名字、无地下室概念 |
| 房间渐进解锁 | 未实现 | `EnsureRooms()` 硬编码一次创建 10 间；无天数/资源/能力条件解锁 |
| 房间管理策略 | 部分实现 | 拖拽换房/分配已实现；**双人房容量（1/2）与同住已实现**（`RoomAvatarProperty.allowDoubleOccupancy` 决定容量，`OccupantIds.Count < capacity` 判定可分配）；隔离/封闭楼层/单人房强制策略未实现 |
| 食物系统 | 部分实现 | 每人 1 单位/天消耗已实现；**厨师/农民产出缺失，食物为 0 时侵蚀 +5 无监听器** |
| 货币系统 | 已实现 | 货币资源定义与初始化（`Currency.asset` + `SettlementBridge` 资源初始化）已实现，旧档 medicine→currency 迁移；事件选项负资源效果有负担校验；**商贩/杂务产出与消费端仍缺** |

### 6. 物资/资源/道具（部分）

| 功能 | 状态 | 说明 |
|------|------|------|
| 食物 | 已实现 | 消耗/产出/UI 完整 |
| 货币 | 已实现 | 货币资源定义（`Currency.asset`）与初始化（`SettlementBridge`）已实现，旧档 medicine→currency 迁移；事件选项货币成本在 UI 与结算两侧均有负担校验；**商贩事件 `choiceEffects` 为空**，无独立产出/消费端 |
| 设施耐久度 | 未实现 | 无数据结构，工程师/木工维护逻辑缺失（明确延期，见文末） |
| 药品 | 未实现（符合策划） | 已迁移为货币，正确移除 |
| 道具系统（P1） | 未实现 | 无道具定义/持有/使用，无「精神分析仪」 |

> 事件配置资产修复：多个 `EventConfig` 资产已修复，为选项补全 `choiceEffects`（含货币/食物负资源成本）与效果 `target`（如 `AllAssignedTenants`），使选项可负担性校验与目标解析可正确生效。

### 7. 驱逐系统（完全未实现）

| 功能 | 状态 | 说明 |
|------|------|------|
| 主动驱逐 | 未实现 | 无驱逐入口/`RemoveTenantChange`/Reducer 分支 |
| 默默离开（绿） | 未实现 | 无 Eviction 日志分类 |
| 哀求留下（黄） | 未实现 | 无士气字段 |
| 攻击疯狂（红） | 未实现 | 无受伤/设施损坏字段 |
| 散布谣言（性格） | 未实现 | 无性格标签字段 |

### 8. 事件系统（框架就绪）

| 功能 | 状态 | 说明 |
|------|------|------|
| 夜间事件（28 个/6 类） | 部分实现 | 实际 27 个，侵蚀强化类缺 1 个 |
| 日间事件（18 个/3 类） | 已实现 | 完整 |
| 特殊 NPC 事件（P2） | 未实现 | 选择器排除非 Normal 事件，无资产 |
| 房客个人事件（13 条） | 未实现 | 仅字段定义，无资产/调度器 |
| 连续叙事链（6 条） | 未实现 | 仅字段定义，无 ChainManager |
| 事件触发逻辑 | 已实现 | 黑夜必触发 + 日间 50% + 条件评估 + 天数窗口 |
| 事件队列与阶段门 | 已实现 | 队列串行派发；待评审/待分配时挂起阶段门，完成后释放；重入（重复 OnPhaseEntered）不会重放计划 |
| 结算重试与重入保护 | 已实现 | 结算失败保留载荷按 `MaxSettleRetries` 限次重试；未决期间忽略重入请求；阶段变更时补结算；超限放弃并标记待清理 |
| 选项负担与能力守卫 | 已实现 | 负资源效果负担（`EventAffordability.CanAfford`）与能力标签（`TenantAbilityResolver.HasAllRequiredTags`）在 UI（按钮置灰）与结算（拒绝重开弹窗）两侧双重校验 |
| Buff 创建/结算/到期 | 已实现 | `ApplyBuff` 创建（唯一 BuffId、冻结目标列表、Dawn 结算）；`TickBuffs` 逐 tick 校验冻结目标仍入住、侵蚀/资源变更、`RemainingTicks<=1` 到期移除，整批原子提交失败顺延下一黎明 |
| 事件与能力标签关系 | 部分实现 | 当前为**硬门禁**（灰掉无标签选项），与策划「代价降低」意图相反 |

### 9. 存档系统（全部实现）

| 功能 | 状态 | 说明 |
|------|------|------|
| 黎明自动存档 | 已实现 | `SettlementBridge.cs:137-142` |
| 3 个独立存档 + 覆盖写入 | 已实现 | `SaveGameService.cs:62,69,83-116` |
| 启动手动选择载入 | 已实现 | `MainMenuController.cs` |
| 无手动存/读档、可删除、不可复制 | 已实现 | 带二次确认的删除 |
| 深拷贝与还原校验 | 已实现 | `RunSaveCodec` 全切片深拷贝（`OccupantIds`/`TargetTenantIds` 重建列表）；读取校验 `SchemaVersion`/`RunId`；事件历史按 `EventId` 去重保留更有意义条目，Buff `LastTickDay` 越界钳制到当前天 |

### 10. 结算与结局（逻辑全空）

| 功能 | 状态 | 说明 |
|------|------|------|
| 结算触发（30 天黑夜结束） | 未实现 | 无 GameOver/EndGame 逻辑 |
| 结算维度计算 | 部分实现 | 仅 `RunSummaryState` 字段 + 序列化；无计算/写入代码 |
| 结局判定（好/普通/坏） | 未实现 | 无结局文本/判定/面板 |
| 奖章商店（低优先级） | 未实现 | 无奖章/商店系统 |
| 多周目高难 | 未实现 | 无周目计数/难度修饰器 |

## 暂缓/不支持机制（明确延期）

以下机制当前明确不实现，检查报告不得将其记录为已支持：

| 机制 | 说明 |
|------|------|
| 设施耐久度 | 无耐久数据结构与维护逻辑（工程师/木工相关） |
| 房间锁定 | 无房间锁定/强制单人间策略（双人房容量 1/2 已实现，但不含锁定） |
| 效率 | 日夜活跃类型无效率/惩罚游戏效果（标签三色仅用于事件条件与选项守卫） |
| 天气/暴风雪（IsStorm 生产者） | `IsStorm` 仅作为存档字段与事件条件枚举，无生产者/天气系统 |
| 线索/信任/陷阱 | 无线索收集、信任值、陷阱玩法 |
| 探索禁用 | 无探索机制与禁用逻辑 |
| 访客生成 | 无特殊访客生成系统（普通评审调度已实现） |
| 强制驱逐 | 无驱逐入口/Change/Reducer/反应逻辑 |

## 回归测试与清理说明

双人间/事件/Buff 工作的编辑器回归测试套件（`Hotel.Runtime.Tests`，7 个测试文件）在清理前全部通过（**105/105**），验证完成后按用户要求从仓库移除，当前仓库**不再包含任何测试代码**。本报告通过日期为 2026-08-14，测试移除前完成。

## 核心缺口（按优先级）

1. **工作系统**（岗位 + 产出 + 分配）——经营闭环核心，完全缺失
2. **驱逐系统**——完全缺失
3. **结算/结局**——30 天结束无触发，游戏无法「通关」
4. **时间 30 天结束机制**——与结算绑定
5. **真实颜色揭示**、**道具系统**、**房间渐进解锁**、**叙事链/个人事件**

## 总结

**存档、审查、时间循环、事件框架、基础侵蚀度已可用**，但「工作产出 → 资源 → 解锁 → 驱逐 → 30 天结算」的经营闭环尚未打通，其中**工作系统**与**驱逐系统**是两块最大的空白。
